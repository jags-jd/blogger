<h1> List of Tags </h1><br />


<ul>
    <% @tags.each do |tag| %>
    <li>
    <%= link_to tag.name, tag_path(tag) %> 
    <%= link_to "delete", tag_path(tag), method: :delete, data: {confirm: "Are you sure you want to delete?"} %><br />
 </li>
    <% end %>
</ul>